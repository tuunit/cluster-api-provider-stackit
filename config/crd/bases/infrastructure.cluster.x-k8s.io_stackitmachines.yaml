---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: stackitmachines.infrastructure.cluster.x-k8s.io
spec:
  group: infrastructure.cluster.x-k8s.io
  names:
    categories:
    - cluster-api
    kind: StackitMachine
    listKind: StackitMachineList
    plural: stackitmachines
    singular: stackitmachine
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: StackitMachine is the Schema for the stackitmachines API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of StackitMachine
            properties:
              availabilityZone:
                description: availabilityZone identifies the STACKIT availability
                  zone for the machine.
                minLength: 1
                type: string
              bootVolumeSizeGiB:
                description: bootVolumeSizeGiB is the size of the machine boot volume
                  in GiB.
                format: int64
                minimum: 1
                type: integer
              imageID:
                description: imageID identifies the STACKIT image used as the boot
                  volume source for the machine.
                minLength: 1
                type: string
              instanceID:
                description: instanceID references the STACKIT instance backing this
                  machine when one already exists.
                minLength: 1
                type: string
              machineType:
                description: machineType identifies the STACKIT instance flavor to
                  use for the machine.
                minLength: 1
                type: string
              providerID:
                description: providerID must match the provider ID as seen on the
                  corresponding Kubernetes Node object.
                minLength: 1
                type: string
            required:
            - availabilityZone
            - bootVolumeSizeGiB
            - imageID
            - machineType
            type: object
          status:
            description: status defines the observed state of StackitMachine
            properties:
              addresses:
                description: addresses contains the associated addresses for the machine.
                items:
                  description: MachineAddress contains information for the node's
                    address.
                  properties:
                    address:
                      description: address is the machine address.
                      maxLength: 256
                      minLength: 1
                      type: string
                    type:
                      description: type is the machine address type, one of Hostname,
                        ExternalIP, InternalIP, ExternalDNS or InternalDNS.
                      enum:
                      - Hostname
                      - ExternalIP
                      - InternalIP
                      - ExternalDNS
                      - InternalDNS
                      type: string
                  required:
                  - address
                  - type
                  type: object
                type: array
              conditions:
                description: |-
                  conditions represent the current state of the StackitMachine resource.
                  Each condition has a unique type and reflects the status of a specific aspect of the resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              failureDomain:
                description: failureDomain is the failure domain where this machine
                  has been placed.
                type: string
              initialization:
                description: initialization provides observations of the StackitMachine
                  initialization process.
                minProperties: 1
                properties:
                  provisioned:
                    description: provisioned is true when the machine infrastructure
                      is fully provisioned.
                    type: boolean
                type: object
              instanceID:
                description: instanceID is the STACKIT server ID managed for this
                  machine.
                type: string
              projectID:
                description: projectID is the STACKIT project used for the last successful
                  reconciliation.
                type: string
              region:
                description: region is the STACKIT region used for the last successful
                  reconciliation.
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
